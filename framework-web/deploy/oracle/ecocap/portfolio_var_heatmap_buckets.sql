DROP TABLE PORTFOLIO_VAR_HEATMAP_BUCKETS;

CREATE TABLE PORTFOLIO_VAR_HEATMAP_BUCKETS
             ( 
                          PORTFOLIO_PD_BUCKET  VARCHAR2(50), 
                          PORTFOLIO_LGD_BUCKET VARCHAR2(50), 
                          VERSION              VARCHAR2(50)
             );
