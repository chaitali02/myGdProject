
export enum Stage{
	//private displayName = any,
	//OutStarted='Not Started',InProgress='In Progress', Completed='Completed', Failed='Failed', Suspend='Suspend',Killed='Killed',login='login',logout='logout',expired='expired',active='active', Inactive='inactive', OnHold='OnHold', OffHold='OffHold', Resume='Resume', Terminating='Terminating'	
}
export class Status{

	/*
	export enum Stage {
			OutStarted="Not Started",
			InProgress="In Progress", Completed="Completed", Failed="Failed", Suspend="Suspend",Killed="Killed",login="login",logout="logout",expired="expired",active="active", Inactive="inactive", OnHold="OnHold", OffHold="OffHold", Resume="Resume", Terminating="Terminating"
		
			private displayName : any;
		public displayName() : any {
			return displayName;
		}
	/*	@Override
		public String toString(){
			return displayName;
		}
	
		Stage(String displayName){
				this.displayName = displayName;
			}
		}

	}
	
	//	private stage : Stage;
   // private createdOn : Date;
	 public Status()
	 {
		 
	 }
	 */
}
